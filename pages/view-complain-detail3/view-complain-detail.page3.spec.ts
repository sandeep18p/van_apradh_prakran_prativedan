import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ViewComplainDetailPage3 } from './view-complain-detail.page3';

describe('ViewComplainDetailPage3', () => {
  let component: ViewComplainDetailPage3;
  let fixture: ComponentFixture<ViewComplainDetailPage3>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ViewComplainDetailPage3);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
